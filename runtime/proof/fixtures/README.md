# Runtime Proof Fixtures

These fixtures document the local card-loop proof shape.

They are sanitized examples for the local harness in `runtime/scripts/`.
They are not live hosted backend responses.

Run the local proof:

```bash
npm --prefix runtime run proof:card-loop
```

Fixture session `124661` is a test fixture only. Runtime code must still use the
active session ID it resolves at run time.
