# Blender Sources

These are the original authored Blender source files for the public Core
runtime assets. Their original filenames are preserved.

The automatic `take2blend.blend1` backup is intentionally omitted. Use the
checked-in `.blend` files as the editable sources.

`Center Sculpture.blend` may require a local relink to the environment files
under `../valen-hdri`.
